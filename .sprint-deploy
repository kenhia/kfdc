# Declares that this repo deploys as part of /sprint-ship (Phase 7).
# One skill name per line; sprint-ship invokes each in order after the merge
# and local cleanup, so what ships is published from the merged main.
#
# Absent file = phase skipped silently, which is how sprint 005's deploy came
# to be run by hand (korg #1035).
#
# See .claude/skills/deploy-board/SKILL.md
deploy-board
